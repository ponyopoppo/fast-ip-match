{
  "name": "fast-ip-match",
  "version": "1.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "ts-mocha test/index.ts",
    "build": "tsc",
    "prepublishOnly": "rm -rf dist && rm -rf node_modules && yarn && yarn build"
  },
  "devDependencies": {
    "@types/assert": "^1.5.4",
    "@types/mocha": "^8.2.2",
    "@types/node": "^15.6.2",
    "assert": "^2.0.0",
    "mocha": "^8.4.0",
    "ts-mocha": "^8.0.0",
    "typescript": "^4.3.2"
  }
}
